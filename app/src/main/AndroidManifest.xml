<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.taozen.quithabit">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/MaterialTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity
            android:name=".NavigationDrawer.Main2Activity"
            android:label="@string/title_activity_main2"
            android:theme="@style/MaterialTheme.NoActionBar"></activity>
        <activity
            android:name=".ProgressCard.FailLogsActivity"
            tools:ignore="InnerclassSeparator" />
        <activity
            android:name=".ProgressCard.ProgressActivity_HerokuStyleFetching"
            tools:ignore="InnerclassSeparator" />
        <activity android:name=".Old.ProgressActivity" />
        <activity android:name=".MainActivity"
            android:theme="@style/MaterialTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Intro.IntroActivity"
            android:label="app_intro" />
        <activity android:name=".Old.ProgressKotlinActivity" />
        <activity android:name=".Test" />
        <activity android:name=".ProgressCard.SavingsActivity" />
    </application>

</manifest>